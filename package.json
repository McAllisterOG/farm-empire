{
  "name": "paradise-isle",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "天堂小岛 Paradise Isle — an original open-source island life-sim inspired by the 2011 web game QQ天堂岛. No Tencent assets; all art is procedurally drawn.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "preview": "vite preview",
    "server": "node server/index.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
